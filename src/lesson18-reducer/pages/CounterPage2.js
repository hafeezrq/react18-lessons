import React from 'react';
import Counter2 from '../components/Counter2';

function CounterPage2({ initialCount }) {
  return <Counter2 initialCount={initialCount} />;
}

export default CounterPage2;
